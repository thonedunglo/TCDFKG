
---

### `.env.example`
```bash
# Python path để CLI nhìn thấy package
PYTHONPATH=src

# Thiết bị
DEVICE=cpu       # hoặc cuda:0

# Đường dẫn chính
DATA_DIR=./data
SAVE_DIR=./artifacts

# Ngẫu nhiên
SEED=42

# Cấu hình mặc định (CLI có thể override)
DATASET_YAML=configs/dataset.yaml
MODEL_CAUSAL_YAML=configs/model_causal.yaml
MODEL_ANOM_YAML=configs/model_anom.yaml
TRAINING_YAML=configs/training.yaml
